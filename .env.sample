# Telegram
BOT_TOKEN=123456:ABCDEF-your-token

# DB (Supabase/PostgreSQL) — полная строка подключения
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Базовые настройки
DEFAULT_TZ=Europe/Moscow
OWNER_USER_ID=000000000  # твой Telegram user id (для "критичных" команд в группах)
SCHEDULER_INTERVAL_SEC=10
BATCH_LIMIT=50

# Безопасный HTML режим
PARSE_MODE=HTML
